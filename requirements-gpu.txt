# Colab environments already have torch, torchvision, and torchaudio pre-installed with CUDA support.
# Do NOT reinstall torch here as it can cause conflicts and slow down setup.

transformers>=4.38.0
peft>=0.9.0
trl>=0.7.11
bitsandbytes>=0.42.0
accelerate>=0.27.2
datasets>=2.17.1
fastapi
uvicorn
pydantic
httpx
